# API-credit plan — no amount requested yet

No credits are spent or redeemed by this package. No budget amount is justified before a pilot.

Use a preregistered set of representative open-source maintenance tasks. Define configurations from the runtime catalog, repetitions, quality rubric, acceptable failures, and stopping conditions before execution. Compare native continuation, observation-only, explicit handoff, and recovery using identical task definitions. Preserve failures and incomplete/censored runs in the results.

Compute a request only after measured pilot costs:

```
requested_budget = preregistered_tasks
                 * configurations_per_task
                 * repetitions
                 * median_observed_cost
                 * reserve_factor
```

All variables are currently UNKNOWN. Record actual cost source/date, input/output/cached token semantics, runtime/model/service tier, and currency. Do not convert account remaining percentages into token balances or apply a single price to different token classes. Recheck current official pricing and program terms before making a monetary request.

Maintenance priorities: representative maintenance comparisons; recovery/cross-model validation; real project fixes and release checks; a bounded rerun reserve. Determine allocations from the pilot, not invented usage statistics. Require explicit user authorization before any spending or reset redemption.
